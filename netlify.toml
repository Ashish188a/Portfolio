# Netlify configuration for Ashish Kumar Xess Portfolio
[build]
  base    = "frontend"
  publish = "dist"
  command = "npm run build"

# React Router SPA — redirect all URLs to index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
